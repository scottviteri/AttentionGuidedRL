# CUDA-enabled PyTorch is required - this project assumes GPU availability
torch>=2.0.0  # CUDA-enabled PyTorch (install with: pip install torch --index-url https://download.pytorch.org/whl/cu118)
transformers>=4.45.0  # Recent version with correct BitsAndBytesConfig import
datasets>=2.13.0
peft>=0.4.0
tqdm>=4.66.0
numpy>=1.24.0
pytest>=7.0.0
bitsandbytes>=0.40.0  # Requires CUDA for quantization
accelerate>=0.22.0  # For device management
scipy>=1.10.0
matplotlib>=3.7.0
pandas>=2.0.0
toolz>=0.12.0
wandb>=0.15.0  # For experiment tracking and logging 